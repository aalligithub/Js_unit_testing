function sum(a, b) {
    return a + b
}

// This function serves no porpuse other than showing changes in coverage
function balls(text){
    return "Very big " + text
}

module.exports = {
    sum: sum,
    balls: balls,
};